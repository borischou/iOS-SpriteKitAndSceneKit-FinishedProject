# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'CombinedSpriteKitSceneKit' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CombinedSpriteKitSceneKit
  pod 'Spine', :git => 'git@github.com:borischou/Spine.git'

end
